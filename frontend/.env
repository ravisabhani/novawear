# Intentionally left blank in repository. Add a local .env.local (ignored) or
# configure the VITE_API_URL in your hosting provider (e.g. Vercel) for builds.
# Example (do NOT commit):
# VITE_API_URL=http://localhost:5000/api
